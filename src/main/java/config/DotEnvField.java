package config;

public enum DotEnvField {
    ENVIRONMENT,
    BROWSER,
    BROWSER_VERSION,
    SERVER_URL,
    PLATFORM,
    MODE,
    SCREEN_WIDTH,
    SCREEN_HEIGHT,
    TIMEOUT,
    POOLING_TIME
}